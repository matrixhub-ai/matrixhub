name: Build Image Latest

on:
  push:
    branches:
      - 'main'

permissions: write-all

jobs:
  call-workflow:
    uses: ./.github/workflows/call-release-image.yaml
    with:
      ref: 'main'
      tagoverride: 'latest'
      add_sha_tag: true
    secrets: inherit
